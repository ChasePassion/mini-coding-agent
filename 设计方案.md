# Minimal Coding Agent 产品方案

## 1. 产品定位

实现一个具备基础自主执行能力、安全控制能力和并发调度能力的 Coding Agent。

Agent 可以：

* 浏览目录
* 读取文件
* 创建、修改文件
* 执行 Shell 命令
* 主动向用户提问
* 并行调用不存在资源冲突的工具
* 对存在冲突的工具自动排队
* 根据运行模式控制文件访问范围及危险操作权限
* 感知当前工具执行状态，并根据工具结果继续 Agent Loop

整个项目按照面试要求分为两个阶段：

### Phase 1：Minimal Coding Agent

交互方式：

* TUI

核心目标：

> 完成一个能够实际执行 Coding Task 的最小 Agent。

### Phase 2：Interactive Coding Agent

在 Phase 1 基础上继续扩展：

* WebUI
* 工具并发
* 工具调度
* 文件锁
* 工具权限确认
* Full Access Mode
* ask_user
* Agent 工具状态感知

Phase 1 完成后必须先提交 Git Commit，再开始 Phase 2。

---

# 2. 启动流程

用户首先进入准备工作的目录：

```text
cd project
```

然后执行 Agent 启动命令。

当前目录自动成为：

```text
Workspace
```

例如：

```text
/home/user/project
```

则：

```text
Workspace = /home/user/project
```

Agent 默认进入：

```text
Default Mode
```

Phase 1 直接启动 TUI。

Phase 2 启动时提供交互界面选择：

```text
Select Interface

> TUI
  WebUI
```

用户通过：

* ↑
* ↓
* Enter

完成选择。

---

# 3. 两种运行模式

整个产品只提供两个模式。

## 3.1 Default Mode

默认安全模式。

具有两个约束。

### 文件系统约束

Agent 只能访问 Workspace 内部。

例如：

```text
Workspace:
/home/user/project
```

允许：

```text
/home/user/project/src/main.py
/home/user/project/README.md
```

禁止：

```text
/home/user/test.txt
/etc/passwd
../other-project
```

该限制对以下工具强制执行：

* read
* write
* list

越界访问直接拒绝。

bash 采取 best-effort 边界：

Shell 命令的文件副作用无法可靠预测，在不引入容器级沙箱的前提下无法强制约束。

因此 bash：

* cwd 固定为 Workspace
* 边界要求写入系统提示词

即：

> read / write / list 是硬边界；bash 是 best-effort 软约束。

### 工具权限约束

以下工具可以直接运行：

* read
* list

以下工具需要用户授权：

* write
* bash

---

# 3.2 Full Access Mode

完全权限模式解除两类限制。

### 文件系统

Agent 可以访问 Workspace 外部。

Workspace 仍然作为：

```text
默认工作目录
```

但不再作为安全边界。

### 工具权限

以下工具无需用户确认：

* write
* bash

直接执行。

因此 Full Access Mode 的含义统一为：

> Agent 获得完整文件系统访问能力以及危险工具自动执行权限。

---

# 4. Agent 工具

Agent 默认具有四个基础工具：

```text
read
write
list
bash
```

Phase 2 增加：

```text
ask_user
```

ask_user 可以由用户开启或者关闭。

---

# 5. read

用于读取文件。

支持：

* 读取完整文件
* 读取指定部分文件
* 多个 read 并行执行

例如：

```text
read README.md
read src/main.py
```

read 不修改文件，因此 Default Mode 下不需要用户批准。

---

# 6. write

用于创建或者修改文件。

支持：

* 新建文件
* 修改已有文件
* 覆盖文件内容

Default Mode 下，每次 write 执行前需要经过权限确认。

例如：

```text
Agent wants to modify:

src/main.py
```

弹出三个选项：

```text
> Allow Once
  Allow for Session
  Deny
```

### Allow Once

只允许当前这一次调用。

### Allow for Session

当前 Agent Session 内后续 write 自动批准。

重新开启 Session 后失效。

### Deny

拒绝本次调用。

拒绝结果会作为 Tool Result 返回 Agent。

---

# 7. list

用于获取目录内容。

例如：

```text
list .
```

返回：

```text
README.md
package.json
src/
tests/
```

支持：

* Workspace 根目录
* Workspace 子目录
* Full Access Mode 下的外部目录

Default Mode 下不需要权限确认。

list 不参与普通文件读写锁。

但是：

> Bash 正在执行时，list 同样不能执行。

因为 Bash 使用全局独占执行机制。

---

# 8. bash

bash 用于执行 Shell 命令。

例如：

```text
python test.py
npm test
git status
```

Default Mode 下需要用户批准。

权限弹窗同样提供：

```text
> Allow Once
  Allow for Session
  Deny
```

其中：

```text
Allow for Session
```

代表：

> 当前 Session 内后续 Bash 调用无需再次确认。

Full Access Mode 下直接执行。

---

# 9. Bash 独占原则

Bash 不允许并发执行。

而且不仅仅是：

```text
bash × bash
```

禁止并发。

规则是：

> Bash 执行期间，整个 Tool Execution System 中有且只能存在 Bash 一个正在运行的工具。

即：

```text
bash + bash   ×
bash + read   ×
bash + write  ×
bash + list   ×
```

全部禁止。

原因是 Bash 的实际文件副作用通常无法可靠预测。

例如：

```text
python script.py
npm install
make
```

可能间接读取或者修改大量文件。

因此不尝试静态推断 Bash 会影响哪些文件。

直接采用：

```text
Global Exclusive Execution
```

---

# 10. Bash 调度流程

Bash 的权限请求不能一产生就立刻弹窗。

完整流程为：

```text
Bash Tool Request
        ↓
检查当前 Tool Scheduler
        ↓
当前是否存在正在执行的工具？
        ↓
YES
        ↓
进入等待队列
        ↓
等待全部工具完成
        ↓
重新确认 Bash 获得执行资格
        ↓
Default Mode？
        ↓
YES
        ↓
向用户请求权限
        ↓
用户批准
        ↓
获取全局独占锁
        ↓
执行 Bash
        ↓
释放全局独占锁
```

也就是说：

> 先确定 Bash 已经轮到执行，再打扰用户进行权限确认。

避免出现：

用户批准 Bash：

```text
npm test
```

结果 Bash 还要在后台等待十几秒才能真正执行。

---

# 11. Bash Approval Reservation

这里需要额外处理一个竞态。

假设：

```text
当前所有工具执行完成
```

于是 Scheduler 判断：

```text
Bash 可以执行
```

随后向用户弹出权限请求。

用户可能十秒以后才点击：

```text
Allow Once
```

这十秒内不能让新的 read/write/list 开始执行。

否则 Bash 获得批准时系统又重新产生了冲突。

因此 Bash 进入权限确认阶段后：

```text
Scheduler
```

为 Bash 保留执行权。

此时：

```text
新的 read
新的 write
新的 list
新的 bash
```

全部进入等待队列。

注意：

这时候 Bash **还没有获取文件锁或者执行锁**。

因此顺序仍然满足：

```text
先得到用户批准
↓
再真正加锁
```

如果用户选择：

```text
Deny
```

则立即释放 Scheduler Reservation，并继续执行后面的工具。

---

# 12. read / write 文件锁

除 Bash 外，read 和 write 使用文件级读写锁。

---

## 12.1 read

read 某个文件之前：

```text
Shared Lock(file)
```

例如：

```text
read main.py
```

获得：

```text
Shared Lock(main.py)
```

此时允许：

```text
read main.py
read main.py
read main.py
```

同时运行。

但是：

```text
write main.py
```

必须等待。

当当前所有：

```text
read main.py
```

全部执行完成之后：

```text
Shared Lock(main.py)
```

释放。

之后 write 才可以执行。

---

# 12.2 write

write 执行前首先获得：

```text
Exclusive Lock(file)
```

例如：

```text
write main.py
```

期间禁止：

```text
read main.py
write main.py
```

执行。

其他不存在资源冲突的文件仍然可以执行。

例如：

```text
write main.py
read README.md
read config.json
```

可以并行。

---

# 13. write 权限与锁顺序

write 必须：

```text
Tool Request
↓
Permission Check
↓
User Approval
↓
等待资源可用
↓
Acquire Exclusive Lock
↓
Execute
↓
Release Lock
```

绝对不能：

```text
Acquire Lock
↓
等待用户批准
```

否则用户长时间不处理权限弹窗，会无意义地锁住资源。

---

# 14. 工具冲突不报错，而是等待

资源发生冲突时，不向 Agent 返回：

```text
Tool Error
File Locked
```

而应该由 Scheduler 自动处理。

例如 Agent 同时产生：

```text
read A
read A
write A
read B
```

实际调度：

```text
read A ──────┐
read A ──────┤ 并行
read B ──────┘

write A
   ↓
等待 read A 全部完成
   ↓
执行
```

Agent 不需要重新调用 write。

因此：

> 并发冲突属于 Tool Scheduler 的职责，而不是 LLM 的职责。

---

# 15. Tool Scheduler

所有 Tool Call 都首先进入统一调度器。

结构：

```text
Agent
  ↓
Tool Request
  ↓
Permission Layer
  ↓
Tool Scheduler
  ↓
Resource / Lock Manager
  ↓
Tool Executor
```

Scheduler 负责：

* 判断工具能否执行
* 判断资源冲突
* 管理等待队列
* Bash 全局独占
* read/write 文件锁
* 并发调度
* 工具状态同步

---

# 16. 并发规则总表

| 当前工具    | 新 read | 新 write | 新 list | 新 bash |
| ------- | -----: | ------: | -----: | -----: |
| read A  |      ✅ |   同文件等待 |      ✅ |     等待 |
| write A |  同文件等待 |   同文件等待 |      ✅ |     等待 |
| list    |      ✅ |       ✅ |      ✅ |     等待 |
| bash    |     等待 |      等待 |     等待 |     等待 |

不同文件之间：

```text
read A + write B
```

允许。

```text
write A + write B
```

允许。

```text
read A + read A
```

允许。

```text
read A + write A
```

不允许。

```text
write A + write A
```

不允许。

---

# 17. 工具状态

用户界面只展示三个正式执行状态。

## Running

图标：

```text
◌
```

持续旋转。

表示工具正在执行。

例如：

```text
◌ Reading src/main.py
```

---

## Success

图标：

```text
✓
```

例如：

```text
✓ Read src/main.py
```

---

## Failed

图标：

```text
×
```

例如：

```text
× Bash: npm test
```

可以展开查看失败原因。

---

# 18. Waiting 的处理

Waiting 不属于正式的 Tool Execution Status。

因为工具此时尚未开始执行。

等待中的 Tool Call 放在 Scheduler Queue 中展示，例如：

```text
Waiting · write src/main.py
```

一旦真正开始执行：

```text
◌ write src/main.py
```

然后最终进入：

```text
✓
```

或者：

```text
×
```

因此用户看到的执行生命周期仍然只有：

```text
Running
Success
Failed
```

Waiting 属于：

```text
Scheduling State
```

而不是：

```text
Execution State
```

---

# 19. Agent 必须感知工具状态

Agent 不应该只收到：

```text
Tool Result
```

还需要知道当前工具执行环境。

Agent 可以感知：

* 哪些 Tool Call 已完成
* 哪些 Tool Call 执行失败
* 哪些 Tool Call 正在运行
* 哪些 Tool Call 正在 Scheduler Queue 中等待
* 哪些 Tool Call 被用户拒绝
* 是否正在等待用户回答 ask_user

例如 Agent 发出：

```text
read package.json
read README.md
write summary.md
```

Agent 可以知道：

```text
read package.json → Success
read README.md → Running
write summary.md → Waiting
```

只有满足当前 reasoning step 所需要的 Tool Result 后，才继续下一轮 reasoning。

这样 Agent 不会错误地认为：

> Tool Call 已发出 = Tool Call 已完成。

---

# 20. ask_user

Phase 2 增加：

```text
ask_user
```

用于 Agent 主动向用户请求信息。

例如：

```text
Which language should I use?
```

Agent 可以提供：

```text
Python
TypeScript
JavaScript
```

用户弹窗显示：

```text
Which language should I use?

> Python
  TypeScript
  JavaScript
  Custom Input...
```

始终保留：

```text
Custom Input...
```

用户选择后可以自由输入内容。

---

# 21. ask_user 对 Agent Loop 的影响

Agent 调用：

```text
ask_user
```

之后：

```text
Agent
↓
ask_user
↓
等待用户输入
↓
用户回答
↓
answer 作为 Tool Result 返回
↓
Agent Loop 继续
```

Agent 在等待用户回答时停止新的 reasoning。

已经开始执行的其他 Tool Call 可以正常完成。

---

# 22. ask_user 开关

用户可以随时控制：

```text
Ask User Tool

ON / OFF
```

当：

```text
ON
```

Agent Tool Context 中存在：

```text
ask_user
```

当：

```text
OFF
```

不是简单地让：

```text
ask_user → Permission Denied
```

而是直接：

> 从 Agent 当前可见 Tool Schema / Context 中删除 ask_user。

因此 Agent 本身不知道存在这个工具，也不会尝试调用。

重新开启后：

```text
ask_user
```

重新进入 Agent Tool Context。

---

# 23. TUI

Phase 1 使用 TUI。

Phase 2 继续保留 TUI。

基础布局：

```text
┌─────────────────────────────────────┐
│ Coding Agent                        │
│ Workspace: ~/project                │
│ Default Mode                        │
├─────────────────────────────────────┤
│                                     │
│ User: summarize this project        │
│                                     │
│ Agent: I'll inspect the project.    │
│                                     │
│ ✓ list .                            │
│ ✓ read README.md                    │
│ ◌ read package.json                 │
│                                     │
├─────────────────────────────────────┤
│ >                                   │
└─────────────────────────────────────┘
```

---

# 24. TUI Slash Command

用户在输入框输入：

```text
/
```

后：

输入框下方立即弹出菜单。

例如：

```text
/
┌──────────────────────────┐
│ > /mode                  │
│   /ask-user              │
│   /clear                 │
│   /help                  │
└──────────────────────────┘
```

支持：

```text
↑
↓
Enter
```

进行选择。

继续输入：

```text
/mo
```

菜单自动过滤为：

```text
/mode
```

---

# 25. /mode

执行：

```text
/mode
```

弹出：

```text
Select Agent Mode

> Default Mode
  Full Access Mode
```

Enter 后立即切换。

界面持续显示当前 Mode。

---

# 26. /ask-user

执行：

```text
/ask-user
```

弹出：

```text
Ask User Tool

> Enabled
  Disabled
```

切换后动态改变 Agent Tool Context。

---

# 27. 其他基础 TUI Command

建议最少保留：

```text
/mode
/ask-user
/clear
/help
```

### /clear

清理当前对话 Session。

### /help

显示基础操作和 Slash Command。

不需要为了面试任务继续增加复杂命令系统。

---

# 28. WebUI

Phase 2 新增 WebUI。

整体采用：

```text
左侧 Session
右侧 Conversation
```

布局。

---

# 29. 左侧区域

主要展示 Chat History。

例如：

```text
Coding Agent

+ New Chat

Today

> Build CSV script
  Fix Python bug
  Summarize project

Yesterday

  Create README
  Analyze API
```

功能：

* 新建 Session
* 切换 Session
* 查看历史 Session
* 显示当前 Session

底部可以显示：

```text
Workspace
~/project
```

---

# 30. 右侧主区域

右侧为 Agent 主要工作区域。

顶部：

```text
Project Agent

Default Mode
Ask User: ON
```

点击：

```text
Default Mode
```

可以切换：

```text
Default
Full Access
```

点击：

```text
Ask User
```

可以：

```text
Enable
Disable
```

---

# 31. Conversation

Tool Call 直接嵌入对话流。

例如：

```text
User

Please inspect this project and write a summary.


Agent

I'll inspect the project structure first.


✓ list .

  README.md
  src/
  package.json


✓ read README.md


◌ read package.json


Agent

The project is a...
```

Tool Card 默认保持简洁。

点击后可以展开：

* Tool Name
* Arguments
* Output
* Error
* Duration

---

# 32. Permission UI

write 请求：

```text
Agent wants to modify

src/main.py
```

弹窗：

```text
Allow Once

Allow Writes for This Session

Deny
```

Bash：

```text
Agent wants to run

npm test
```

弹窗：

```text
Allow Once

Allow Bash for This Session

Deny
```

---

# 33. ask_user UI

Agent：

```text
Which language should I use?
```

弹窗：

```text
> Python
  TypeScript
  JavaScript
  Custom Input...
```

如果选择：

```text
Custom Input...
```

变成输入框：

```text
Your answer:

________________________

Submit
```

---

# 34. Agent Loop

完整 Agent Loop：

```text
User Input
    ↓
LLM Reasoning
    ↓
产生 Tool Calls
    ↓
Tool Availability Check
    ↓
Permission Layer
    ↓
Tool Scheduler
    ↓
Resource Lock Manager
    ↓
Tool Execution
    ↓
Tool State / Tool Result
    ↓
返回 Agent Context
    ↓
LLM Reasoning
    ↓
继续 Tool Call / ask_user / Final Answer
```

---

# 35. Agent Context

每一轮 Agent 需要获得：

### 用户上下文

* 当前 Conversation
* 用户任务

### Environment

* Workspace
* Current Mode

### Available Tools

例如：

```text
read
write
list
bash
ask_user
```

如果 ask_user Disabled：

```text
read
write
list
bash
```

### Tool Runtime State

包括当前：

* Running Tools
* Waiting Tool Requests
* Completed Results
* Failed Results
* Rejected Results

从而让 Agent 对实际运行状态拥有认知。

---

# 36. 一个完整工作案例

用户：

```text
分析这个项目，然后给它写一个 README，最后运行测试。
```

Agent：

```text
list .
```

Success。

随后并行：

```text
read package.json
read src/main.py
read src/config.py
```

三个 read 并行。

Agent 获得结果后：

```text
write README.md
```

Default Mode。

弹出：

```text
Allow Once
Allow Writes for This Session
Deny
```

用户：

```text
Allow Once
```

write 获取：

```text
Exclusive Lock(README.md)
```

执行完成。

Agent：

```text
bash npm test
```

Scheduler 发现当前：

```text
没有其他工具执行
```

于是为 Bash 保留调度权。

向用户请求：

```text
Allow Once
Allow Bash for This Session
Deny
```

用户：

```text
Allow Once
```

Bash 获取：

```text
Global Exclusive Lock
```

执行：

```text
npm test
```

期间任何：

```text
read
write
list
bash
```

全部进入 Waiting Queue。

Bash 完成：

```text
✓ npm test
```

释放全局锁。

Scheduler 继续执行等待任务。

Agent 获得测试结果后给用户最终回答。

---

# 37. Phase 1 最终范围

Phase 1 应控制范围，避免一次做得太大。

必须完成：

```text
TUI
Agent Loop
Workspace
Default Mode
Full Access Mode
read
write
list
bash
基础权限控制
基础 Tool Result
```

能够完成：

### Case 1

```text
总结 README.md
```

### Case 2

```text
写一个 Python 脚本统计 CSV 数据。
```

### Case 3

```text
创建脚本并运行它。
```

Phase 1 完成后：

```text
Git Commit
```

形成明确基线。

---

# 38. Phase 2 最终范围

在 Phase 1 基础上增加：

```text
WebUI
Parallel Tool Calls
Tool Scheduler
read/write Lock
Bash Global Exclusive Lock
Waiting Queue
3-option Permission
Session Permission
ask_user
ask_user Dynamic Disable
Agent Runtime State Awareness
TUI Slash Commands
Chat History
Tool Call Visualization
```

---

# 39. 产品设计的核心原则

整个 Agent 可以用五句话解释。

### Workspace 决定 Agent 默认在哪里工作

```text
Workspace
```

是 Agent 的默认工作目录。

### Mode 决定 Agent 能做多大的事情

```text
Default
Full Access
```

同时控制：

* 文件系统范围
* write/bash 权限

### Permission 决定 Agent 是否可以执行危险行为

Default Mode：

```text
write
bash
```

需要用户授权。

### Scheduler 决定 Tool 什么时候执行

负责：

* 并发
* 等待
* 冲突处理
* Bash 独占

### Lock 保证实际执行不会产生文件竞争

```text
read → Shared
write → Exclusive
bash → Global Exclusive
```

最终整个产品形成：

```text
User
 ↓
TUI / WebUI
 ↓
Agent Loop
 ↓
Permission
 ↓
Scheduler
 ↓
Lock Manager
 ↓
Tools
 ↓
Runtime State
 ↓
Agent
```

核心不是简单给 LLM 四五个工具，而是：

> 构建一个具备权限边界、并发调度、状态感知和 Human-in-the-loop 能力的最小 Coding Agent Runtime。
