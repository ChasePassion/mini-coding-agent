{
	"name": "mini-coding-agent",
	"version": "0.1.0",
	"private": true,
	"description": "Minimal coding agent: agent loop + tools + permissions + TUI (interview Phase 1)",
	"type": "module",
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"start": "tsx src/index.ts",
		"smoke": "tsx scripts/smoke.ts",
		"task": "tsx scripts/task.ts",
		"typecheck": "tsc --noEmit",
		"test": "node --import tsx --test \"tests/*.test.ts\""
	},
	"dependencies": {
		"@earendil-works/pi-ai": "^0.84.4",
		"@earendil-works/pi-tui": "^0.84.4",
		"@types/ws": "^8.18.1",
		"ws": "^8.21.3"
	},
	"devDependencies": {
		"@types/node": "^24.0.0",
		"tsx": "^4.19.0",
		"typescript": "^5.6.0"
	}
}
